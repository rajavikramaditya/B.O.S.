{
  "name": "owner_agent_loop",
  "description": "Non-destructive smoke for Cursor-like multi-step agent loop (diagnose/status). No broadcast/TTS/delete.",
  "channel_label": "agent_probe",
  "turns": [
    {
      "id": "diagnose_chain",
      "message": "diagnostics chalao aur batao ab kya karna chahiye",
      "expect": {
        "reply_not_empty": true,
        "max_latency_ms": 90000,
        "forbid_actions": ["send_azuracast", "generate_audio", "SEND_AZURACAST", "GENERATE_AUDIO"],
        "forbid_substrings": ["Temporary runtime context"]
      }
    },
    {
      "id": "pipeline_recommend",
      "message": "pipeline status check karke next step batao",
      "expect": {
        "reply_not_empty": true,
        "max_latency_ms": 90000,
        "forbid_actions": ["send_azuracast", "generate_audio"]
      }
    }
  ]
}
